/**
 * Алиас маршрута.
 *
 * Фронт по умолчанию стучится в /api/whatsapp.php — этот путь остался от
 * PHP-версии и зашит в VITE_API_ENDPOINT по умолчанию. Cloudflare строит
 * маршрут из имени файла, поэтому whatsapp.php.js отвечает на /api/whatsapp.php.
 *
 * Благодаря этому файлу публикация на Cloudflare не требует ни одной
 * переменной сборки: форма работает сразу после деплоя.
 */
export { onRequest } from './whatsapp.js'
