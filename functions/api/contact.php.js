/**
 * Алиас маршрута.
 *
 * Фронт по умолчанию стучится в /api/contact.php — этот путь нужен для
 * обычного PHP-хостинга. Cloudflare строит маршрут из имени файла, поэтому
 * contact.php.js отвечает на тот же адрес, и публикация не требует
 * ни одной переменной сборки.
 */
export { onRequest } from './contact.js'
